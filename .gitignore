.env
.vercel

# Data folder - exclude all data files to prevent public sharing
data/
*.csv
# Exclude data JSON files but keep essential config files
data/*.json
firestore.indexes.json
firestore.rules
